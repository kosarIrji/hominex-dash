export const url = (endpoint: string) =>
  `https://api-hominex.ir/api/v1${endpoint}`;

export const url_v1 = (endpoint: string) =>
  `https://api-hominex.ir/api/v1${endpoint}`;

export const token_name = "authjs.session-token";
